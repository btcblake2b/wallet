import { sdk } from '../sdk'

// // Nessuna action custom: la pagina web copre la configurazione del nodo, la
// // stringa di connessione (URI/QR) e l'autorizzazione di nuovi dispositivi.
export const actions = sdk.Actions.of()
